/** Client poll interval for dashboard refresh (ms) */
export const DASHBOARD_POLL_MS = 30_000;

/** Shell considers data live if asOf is within this window */
export const LIVE_THRESHOLD_MS = 5 * 60 * 1000;
