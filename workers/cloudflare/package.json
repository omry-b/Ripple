{
  "name": "ripple-cloudflare-cron",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --config ../../wrangler.toml",
    "deploy": "wrangler deploy --config ../../wrangler.toml",
    "tail": "wrangler tail --config ../../wrangler.toml"
  },
  "devDependencies": {
    "wrangler": "^4.0.0",
    "@cloudflare/workers-types": "^4.20250200.0",
    "typescript": "^5"
  }
}
